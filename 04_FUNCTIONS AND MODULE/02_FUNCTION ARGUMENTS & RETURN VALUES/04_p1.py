# Write a function full_name(first,last) that takes first name and last name as parameters and return a single a string in the format "First Last"

def full_name(first, last):
    return f"{first} {last}"

name = full_name("Amit", "Das")

print(name)